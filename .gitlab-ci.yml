stages:
  - build
  - test

default:
  image: node

build-job:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - "dist/"

test-job:
  stage: test
  script:
    - npm run test
