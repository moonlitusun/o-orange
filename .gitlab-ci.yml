stages:
  - build
  - test

default:
  image: node:16.10.0

build-job:
  stage: build
  script:
    - npm install --registry "http://npm.web.dz"
    - npm run build
  artifacts:
    paths:
      - "dist/"

test-job:
  stage: test
  script:
    - npm run test
