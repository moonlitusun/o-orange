/**
 * @since 0.0.1
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean}
 */
function isUndefined(value: any): boolean {
  return value === void 0;
}

export default isUndefined;