/**
 * @since 0.0.1
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean}
 * @example
 *
 * isNumber(0)
 * // => true
 *
 * isNumber('0')
 * // => false
 *
 */
declare function isNumber(value: any): boolean;
export default isNumber;
