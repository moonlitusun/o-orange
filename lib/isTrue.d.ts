/**
 * @since 3.1.0
 * @category Finace
 * @param {*} value The value to check.
 * @returns {boolean}
 * @example
 *
 * isTrue(false)
 * // => false
 *
 * isTrue(0)
 * // => true
 *
 */
declare function isTrue(value: any): boolean;
export default isTrue;
